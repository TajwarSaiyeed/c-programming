#include<stdio.h>
#include<math.h>

/*
    Quick Quiz:
        Use a library function to calculate the area
        of a square with side a.
*/


int main() {
    double a =2 ;
    printf("The area of the square is: %lf", pow(a,2));

    return 0;
}
