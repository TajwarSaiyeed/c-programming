/**
 * Create a string using " " and
 * prints its content using a loop
 *
 */

#include <stdio.h>
int main()
{
    char s[] = "Abid";
    for (int i = 0; i < 5; i++)
        printf("%c", s[i]);
    return 0;
}