#include <stdio.h>
/*
    A computer program is a set of instructions.Just like a recipie.
    which contains instructions to prepare a particular dish.
*/

/*
    type of instructions:

    1. Type declaration instruction
    2. Arithmetic instruction
    3. Control instruction
*/

// type declaration instruction

// int a means reserve 4 bytes in the memory for integer
// float b means reserve 4 bytes in the memory for floating point

int main()
{

    int i = 10;
    int j = i;
    int x = 2;

    // this will show error
    // float b = a + 3; we are trying to use "a" before defining it.
    // float a = 1.1

    int d, e, f, g;
    d = e = f = g = 30; // value of d,e,f,g will be 30 each;
}
