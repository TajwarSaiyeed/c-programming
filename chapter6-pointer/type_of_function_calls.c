#include <stdio.h>

int main()
{

    /*
        Types of function calls
        Based on the way we pass arguments to
        the function, function calls are of two
        types.


        Call by value => sending the values of arguments
        Call by reference => sending the "address of" arguments
    */

    return 0;
}
