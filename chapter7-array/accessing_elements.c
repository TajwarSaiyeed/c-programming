#include <stdio.h>

int main()
{
    // Accessing elements
    // Elements of an array can be accessed using

    int marks[3];
    printf("Enter 1 marks: ");
    scanf("%d", &marks[0]);
    printf("Marks one is: %d\n", marks[0]);
    return 0;
}
